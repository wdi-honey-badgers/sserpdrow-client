<div class="container">
    POSTS:
    {{#each object as |posts|}}
      {{#each posts as |post|}}
        <div class="col-xs-6 col-sm-6 col-md-3 col-lg-3 col-xl-3">
          {{#if post.blog}}
          Blog
          {{else}}
          Website
          {{/if}}
          <div>{{post.title}}</div>
          <div>{{post.content}}</div>
          <div>{{post._id}}</div>
          <button data-id={{accomplishment.id}} type="button">Update</button>
          <button data-id={{accomplishment.id}} type="button">Delete</button>
        </div>
      {{/each}}
    {{/each}}
</div>
